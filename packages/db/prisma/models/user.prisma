model User {
  id         String     @id @default(uuid())
  email      String     @unique
  password   String
  name       String?
  imageUrl   String?
  verified   Boolean    @default(false)
  timezone   Timezone   @default(UTC_0)
  unitSystem UnitSystem @default(METRIC)
  pets       Pet[]
}
