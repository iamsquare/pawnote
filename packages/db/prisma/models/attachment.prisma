model Attachment {
  id        String     @id @default(uuid())
  slug      String
  mimeType  String
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
  vaccines  Vaccine[]  @ignore
  documents Document[] @ignore
}
