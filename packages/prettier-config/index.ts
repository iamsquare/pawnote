import type { Config } from 'prettier';

export const prettierConfig: Config = {
  singleQuote: true,
  trailingComma: 'all',
  arrowParens: 'always',
  printWidth: 120,
  tabWidth: 2,
  semi: true,
};
